- op: replace
  path: /spec/source/targetRevision
  value: dev-managment-update # This can be changed if you want to deploy a specific branch or tag (note you will also have to change it on /manifest/argo/namespaces/application.yaml's helm value manually)